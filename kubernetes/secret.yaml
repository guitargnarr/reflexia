apiVersion: v1
kind: Secret
metadata:
  name: reflexia-secrets
  namespace: reflexia
type: Opaque
data:
  # Example API key: "your-api-key-here" encoded in base64
  # To generate, run: echo -n "your-api-key-here" | base64
  API_KEY: "" # Leave empty for no authentication